import * as React from "react";
import { Component } from "react";
import { TouchableOpacity, View, FlatList, Text } from "react-native";
//import { withNavigation } from "react-navigation";

/*
import { useNavigation } from "@react-navigation/native";

import CardHome from "./CardHome";

const navigation = useNavigation();

class ProductosHome extends Component {
  constructor() {
    super();
    this.state = {
      productos: prods
    };
  }

  render() {
    return (
      <View>
        <FlatList
          keyExtractor={result => result.id}
          data={prods}
          renderItem={({ item }) => {
            return (
              <TouchableOpacity
                onPress={() =>
                  this.props.navigation.navigate("Detalle", { prod: item })
                }
              >
                <CardHome data={item} />
              </TouchableOpacity>
            );
          }}
        />
      </View>
    );
  }
}

const prods = [
  {
    id: "1",
    titulo: "Torta de Chocolate",
    foto:
      "https://cocina-casera.com/wp-content/uploads/2018/03/torta-de-chocolate.jpg",
    dueno: "Martina",
    distancia: "5km"
    // ubicacion: { latitude: -34.906112, longitude: -56.186332}
  },
  {
    id: "2",
    titulo: "Hamburguesa",
    foto:
      "https://portal.minervafoods.com/files/styles/blog_post_page/public/como_fazer_hamburguer_caseiro_0.jpg?itok=kxHwP-dY",
    dueno: "Gonzalo",
    distancia: "1km"
  },
  {
    id: "3",
    titulo: "Verduras",
    foto:
      "https://okdiario.com/img/2018/05/11/elegir-bien-las-frutas-y-verduras.jpg",
    dueno: "Juana",
    distancia: "3km"
  }
];

export default withNavigation(ProductosHome);
*/
